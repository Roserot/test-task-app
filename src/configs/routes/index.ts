import {routes as routesPseudos} from '../pseudos/routes';

export const routes = {
  [routesPseudos.MAIN]: {
    path: '/main'
  },
  [routesPseudos.LOGIN]: {
    path: '/login'
  }
};

export default routes;